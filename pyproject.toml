[project]
name = "assignment3"
version = "0.1.0"
description = "GAN on MNIST with FastAPI and Docker"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.2",
    "torchvision>=0.17",
    "fastapi>=0.111",
    "uvicorn>=0.30",
    "pillow>=10.3",
    "python-multipart",
    "numpy>=1.26"
]
